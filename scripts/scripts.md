# 2. 辅助脚本 (`SpecMd/scripts/`)

为了辅助智能体快速获取上下文，提供以下脚本：

- `get_env_name.ps1`: 获取项目当前使用的虚拟环境名称（读取 `.env`）。
- `check_codefile_structure.ps1`: 快速生成并展示 `src` 目录下的代码文件结构。
- `check_specify_structure.ps1`: 快速生成并展示 `specs` 目录下的规范驱动文档结构。
